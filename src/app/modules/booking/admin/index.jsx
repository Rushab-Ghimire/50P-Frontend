import React from "react";
import BookAppointment from "./components/BookAppointment";

const AdminBooking = ({ refreshFx }) => {
  return <BookAppointment refreshFx={refreshFx} />;
};

export default AdminBooking;
