import React from "react";
import QueueAppointment from "./components/QueueAppointment";

const QueueBooking = ({ refreshFx }) => {
  return <QueueAppointment refreshFx={refreshFx} />;
};

export default QueueBooking;
